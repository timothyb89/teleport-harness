# Render context for tbot (see harness/render.py).
bot_token: harness-tbot-secret
# bots created at bootstrap: name / roles / join token (must match a bootstrap token)
bots:
  - {name: test-bot, roles: tbot-tester, token: harness-tbot-secret}
