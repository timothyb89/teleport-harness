{# The test bot's join token (token method, fixed secret = name). bots.manifest's
   `bots add --token` references this existing token. #}
kind: token
version: v2
metadata: {name: {{ bot_token }}}
spec: {roles: [Bot], bot_name: test-bot, join_method: token}
